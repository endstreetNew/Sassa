@page "/ActiveUsers"

@inject ActiveUserList _ConnectedUserList

<BrmPageTitle Title="Active Users"/>

<FluentDataGrid Items="@_ConnectedUserList.Users.AsQueryable()" TGridItem="ActiveUser" Pagination="@pagination">
    <PropertyColumn Width="100px" Property="@(p => p.Name)" Sortable="true" />
</FluentDataGrid>
<FluentPaginator State="@pagination" />
@code
{
    PaginationState pagination = new PaginationState { ItemsPerPage = 20 };

    // protected async Task SendMail(string userName) => await _ConnectedUserList.SendMail(userName);)

}